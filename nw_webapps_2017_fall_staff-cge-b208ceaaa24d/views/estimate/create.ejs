<div class="container-fluid">
  <div class=row>
    <div class="col col-md-12">
      <a href="../estimate/select" class="btn btn-success new">
        COPY from existing estimate...
      </a>
      <br/>
      <br/>
      <h2>Create a new estimate </h2>
      <br/>
      <% maxID=0; for (var i=0; i < estimates.query.length; i++) {
      if(estimates.query[i]._id > maxID) { maxID=estimates.query[i]._id;   }
      var sqft = estimates.query[i].squareFeet;}%>
        <div class="content">
          <div>
            <form name="estimate" method="POST" action="/estimate/save">

              <div class="form-group" id=frm>
                <label for="_id">ID</label>
                <input type="number" class="form-control" id="_id" name="_id" value="<%= maxID+1 %>" required readonly>
              </div>

              <!-- COMMON TO EDIT AND CREATE -->
              <%include partial_edit.ejs%>

                <div class="form-group">
                  <input type="submit" value="Save" class="btn btn-primary" />
                </div>
            </form>

            <br/>

            <form method="get" action="/estimate">
              <input type="submit" value="Return to list" class="btn btn-caution" />
            </form>

          </div>
        </div>
    </div>
  </div>
</div>